# Phase 02 — Code Refactor

## Priority: HIGH
## Status: pending
## Estimated: 1-2 hours

## Overview

Reemplazar strings `maureldev` / `MaurelDev` por `gitcontrol` / `GitControl` en código + docs. Hacer en branch dedicada antes de tocar GitHub repo.

## Branch

```bash
git checkout -b feat/rename-to-gitcontrol
```

## Files to Modify

### Config (2 files)
- `package.json:2` → `"name": "gitcontrol"`
- `docker-compose.dev.yml` → ver Phase 03 (DB migration coordinado)

### App Strings (8 files)

| File | Line | Cambio |
|------|------|--------|
| `src/app/layout.tsx` | 20 | `title: "GitControl — GitHub Dashboard"` |
| `src/app/login/page.tsx` | 43 | `<h1>GitControl</h1>` |
| `src/app/(dashboard)/_components/app-sidebar.tsx` | 110 | `<span>GitControl</span>` |
| `src/app/(dashboard)/changelog/page.tsx` | 9, 61 | metadata + heading |
| `src/app/(dashboard)/changelog/_components/changelog-empty.tsx` | 18 | "The GitControl repo is private..." |
| `src/app/(dashboard)/report-bug/page.tsx` | 8 | `"Report a bug — GitControl"` |
| `src/app/actions/create-bug-report.ts` | 33, 42, 106 | comments + texto issue body |
| `src/lib/github/client.ts` | 7 | `"GitControl/0.1 (+https://github.com/dmaurelc/gitcontrol)"` |
| `src/lib/github/upstream.ts` | 2, 6 | `UPSTREAM_REPO = "gitcontrol"` |
| `src/lib/dependencies/build-issue-body.ts` | 43 | "Generated by GitControl..." |

### Docs (5 files)

- `README.md` — replace MaurelDev → GitControl (mantener "formerly maureldev" nota opcional al final)
- `docs/project-overview-pdr.md` — line 3 + ocurrencias
- `docs/system-architecture.md` — lines 167-168 (post-DB migration)
- `docs/deployment-guide.md` — lines 97-98 (post-DB migration)
- `docs/codebase-summary.md` — line 8 (estructura)

## Implementation Steps

```bash
# 1. Crear branch
git checkout -b feat/rename-to-gitcontrol

# 2. Replace en código (case-sensitive)
# MaurelDev → GitControl
# maureldev → gitcontrol (cuidado: NO tocar plans/reports históricos)

# 3. Lint + typecheck
pnpm lint
pnpm tsc --noEmit

# 4. Build local
pnpm build

# 5. Test local
pnpm dev
# Verificar: title, login page, sidebar, changelog
```

## Strings to Preserve (DO NOT change)

- `plans/**` — historia de planificación
- `docs/project-changelog.md` (si existe) — historial
- `.git/` — historial commits
- Carpeta proyecto en disco (Phase 04 decision)

## Validation

- [ ] `pnpm tsc --noEmit` pasa
- [ ] `pnpm lint` pasa
- [ ] `pnpm build` exitoso
- [ ] Dev server arranca sin errores
- [ ] UI muestra "GitControl" en: tab title, sidebar, login

## Commit

```bash
git add -A
git commit -m "refactor: rename project from MaurelDev to GitControl"
```

## Next Phase

→ Phase 03: Database migration (Postgres local + prod nomenclature)
